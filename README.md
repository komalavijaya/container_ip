# container_ip
Simple shell script to get the IP address of Docker containers

I have developed a simple shell script to get the IP address of an individual container or all the running containers in a host. The script will take command line argument which is either “All” or “name of the Docker container”.

Developer - Janarthanan
